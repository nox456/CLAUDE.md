- When i prompt "Ready for PR" run the scripts defined in @.github/actions/run-checks/action.yaml and run the project unit tests, use the git diff against the working branch and check for: debugging console.logs and useless comments. Also, modify the implementation plan (if any) to be readable by QA reviewers, that is: Removing files modified, Verification steps and just keep non-technical information, create the modified plan at the project's root.
